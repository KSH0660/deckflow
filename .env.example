# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
LLM_MODEL=gpt-5-mini
SUMMARIZATION_MODEL=gpt-5-nano
# Logging Configuration
LOG_LEVEL=INFO

# Repository backend (sqlite|memory)
DECKFLOW_REPO=sqlite

# SQLite DB path (used when DECKFLOW_REPO=sqlite)
DECKFLOW_SQLITE_PATH=decks.db

# Concurrency limits
# Maximum number of decks generating in parallel
DECKFLOW_MAX_DECKS=3
# Maximum concurrent slide generations per deck
DECKFLOW_MAX_SLIDE_CONCURRENCY=3

# PDF export (optional): Playwright requires Chromium install
# Install once: `uv run python -m playwright install chromium`
